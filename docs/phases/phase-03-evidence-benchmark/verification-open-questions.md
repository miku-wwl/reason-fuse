# Phase 3 Verification — Open Questions and Remaining Boundaries

The previous blocking questions are resolved in the v2 construction batch.
The items below are retained for a higher-level model or later production
verification rather than hidden in the PASS report.

## Resolved: scenario diversity

The v2 dataset has 20 explicit semantic pattern families in each category.
Oscillation now covers 20 distinct diagnostic-pair/resource families, and
Retrieval Churn covers 20 distinct incident domains with different query
intents and evidence identities. The independent verifier recomputed this
coverage and no longer blocks Phase 3.

## Resolved: Todo-only negative control

H-018 includes `OPEN -> IN_PROGRESS -> COMPLETED` Todo snapshots without
objective evidence. All three repetitions preserve `todo_delta=true` while
keeping `objective_progress=false` and completing without containment.

## Resolved for bounded execution: model-backed Foundry evaluator

The project already contains the usable Azure model configuration:
`australiaeast`, deployment `gpt-5-mini`, model version `2025-08-07`.
The offline deterministic `F1ScoreEvaluator` completed 300/300 label checks.
After enabling the evaluation SDK's reasoning-model compatibility path
(`is_reasoning_model=True`), `ToolCallAccuracyEvaluator` reached the real
Azure deployment. The initial bounded run completed 10/10 with zero
transport/evaluator errors, followed by the complete 300-case run described
below.

Evidence was generated by the verification script and is intentionally not
retained as a long-term attachment.

The full model-backed run is now complete. The initial 10-case run was
followed by 100 scenarios x 3 repetitions against `gpt-5-mini`; 296 cases
completed in the first pass and four Azure 429 cases were recovered by a
low-concurrency retry. The merged artifact has 300/300 completed and zero
transport/evaluator errors. The model threshold result is 159/300, which is a
quality finding rather than an execution blocker.

The separate top-level PyPI `foundry` module is not the supported project SDK
surface and was not installed. The environment has `azure-ai-evaluation`,
`azure-ai-projects`, `agent_framework.foundry`, and Azure AI/Foundry CLI
extensions. The quality-layer label remains `PARTIAL` because cloud Foundry
evaluation orchestration is not independently evidenced and the model score
is not universally passing.

The first attempt's `max_tokens` error is retained in command evidence as a
resolved SDK/model compatibility finding, not as a missing-configuration
finding.

## Hosted boundaries now evidenced

The following previously retained boundaries now have current hosted evidence:

```text
production Operations backend     PASS
cloud Core trace correlation      PASS
concurrent/forked turns           PASS
fresh-client recovery proxy       PASS
```

## Remaining open questions

```text
ToolCallAccuracy model quality   PARTIAL (159/300 met score >= 3; 0 execution errors)
Foundry IQ native retrieval       NOT VERIFIED
true platform cold-start         NOT VERIFIED
```

The 159/300 result is not a transport failure: every case has a completed
model judgment. It is a quality question for a later model/benchmark review:
determine whether the evaluator prompt, expected tool-call representation,
or the hosted trajectory itself explains the 141 below-threshold judgments.

Foundry project connection discovery returned zero connections and the
subscription has no configured native knowledge source for this project, so
the existing `retrieval_fixture` tool is not being mislabeled as Foundry IQ.
The fresh-client recovery test proves conversation/state restoration, but it
does not force an App Service/agent platform restart and therefore does not
prove true cold-start recovery.

## Reproduction

When evidence is needed again, rerun the Phase 3 dataset, evaluator, and
hosted-boundary verification scripts from a clean evidence directory. The
reports retain the conclusions and unresolved boundaries; raw run logs are
not treated as permanent deliverables.
