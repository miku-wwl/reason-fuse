# Phase 1 evidence

Only real hosted integration runs can establish PASS. Local checks must be labelled
as such. Record UTC time, command, exit code and applicable conversation/session IDs,
external execution counters, affinity headers and SSE arrival times. Never record
credentials, Authorization headers, subscription keys or signed download URLs.

Region: Australia East (`australiaeast`).
